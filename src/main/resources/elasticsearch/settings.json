{
  "analysis": {
    "filter": {
      "synonym_filter": {
        "type": "synonym",
        "synonyms": [
          "롤, 리그오브레전드, lol"
        ]
      }
    },
    "analyzer": {
      "jaso_analyzer": {
        "type": "custom",
        "tokenizer": "jaso_tokenizer"
      },
      "korean_icu_analyzer": {
        "type": "custom",
        "tokenizer": "nori_tokenizer",
        "filter": [
          "lowercase",
          "icu_folding"
        ]
      },
      "synonym_analyzer": {
        "type": "custom",
        "tokenizer": "nori_tokenizer",
        "filter": [
          "lowercase",
          "synonym_filter"
        ]
      }
    }
  }
}
